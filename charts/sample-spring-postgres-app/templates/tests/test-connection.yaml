apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-spring-postgres-app.fullname" . }}-test-connection"
  labels:
    {{- include "sample-spring-postgres-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-spring-postgres-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
